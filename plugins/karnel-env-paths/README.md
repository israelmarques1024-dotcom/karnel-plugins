# karnel-env-paths

Print the active Karnel version and XDG-based config, data, cache, plugin, and
Termux prefix paths. It is useful when diagnosing an installation or locating
Karnel state without modifying it.

```bash
karnel plugin install karnel-env-paths
karnel-env-paths
```
